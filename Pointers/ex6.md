Write the following function:<br>
**int count_occurrences(struct node *list, int n);**<br>
The list parameter points to a linked list; the function<br> 
should return the number of times that n appears in this list. 
